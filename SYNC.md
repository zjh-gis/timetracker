# 账号与设备同步

## 架构

浏览器把当前登录用户的数据保存在独立的 `localStorage` 键中，并通过已登录会话调用同源 `POST /api/sync/`。服务端依据会话中的用户 ID 读写腾讯云 PostgreSQL；客户端不能指定或冒充其他用户 ID。

服务端把分类、任务、时间记录和活动计时器存入规范化表，同时用 `time_sync_state.revision` 维护账号级版本号。所有写入在 PostgreSQL 事务内完成。

## 同步行为

- 首次登录会把有意义的本地旧数据迁移到该账号并上传；新设备没有本地记录时会拉取云端数据；
- 数据变化后约 1.2 秒自动同步，页面恢复可见和每 30 秒也会检查；
- 服务端对同一账号的同步状态加行锁，避免并发写入；
- 本机基准版本落后且本地也有修改时返回 `409 Conflict`，不会静默覆盖；
- 用户可明确选择云端版本或用本机版本覆盖；处理冲突前建议导出 JSON；
- 单次请求最大 2 MB，并限制分类、任务、记录及文本字段数量；
- 登录会话失效后同步接口返回 401，不会退回匿名同步。

当前仍是“整份文档 + 版本号”同步，不是逐条 CRDT 合并。两台设备长期离线并同时编辑仍可能需要人工选择一个版本。

## 数据归属和删除

每张业务表都带 `user_id`，并通过外键关联认证用户。删除用户时，认证会话、任务、记录、同步状态等均由 `ON DELETE CASCADE` 删除。浏览器退出登录后也会清除该账号在当前设备上的本地副本。

## 运维要求

- 只允许应用服务器通过腾讯云私有网络连接数据库；不要向公网开放 PostgreSQL 端口；
- 对数据库启用自动备份和定期恢复演练；
- 应用必须使用 HTTPS；Nginx 应对认证和同步接口增加限流；
- 修改数据库结构时新增迁移文件，不要手工改生产表；
- 多实例部署时应把内存级 API 限流迁移到 Nginx、Redis 或统一网关。
